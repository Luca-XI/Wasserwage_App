# Git Befehle für Wasserwaage

hinzufügen (alle Änderungen) = git add .
hinzufügen (eine Datei)       = git add DATEI

speichern                    = git commit -m "Hier deine Nachricht eintragen"

hochladen                    = git push

ziehen                       = git pull

status                       = git status